package com.artronics.chapar.controller.sdwn.packet;

import com.artronics.chapar.core.entities.Packet;
import org.apache.log4j.Logger;

public class DataPacket extends Packet<SdwnPacketType>{
    private final static Logger log = Logger.getLogger(DataPacket.class);
}
